#apply(M,1,mean) -> M is matrix , 1 = rows of matrix , mean - func we want to apply
#apply(M,1,max)
#we will get a vector with means of each row
#2 -> col #we will get a vector with means of each col


#The apply family has following func :
#1.apply - use on matrix : either the row or col
#2.lappy - apple a func. to ele of a list (L)
#3.sapply - a version of lapply can simplify (S) the results so its not presented as a list

#many more